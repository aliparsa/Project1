<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pga.project1" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/CustomActionBarTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".fragment.PersonelPickerActivity"
            android:label="@string/title_activity_personel_picker" />
        <activity
            android:name=".fragment.NewReportActivity"
            android:label="@string/title_activity_new_report" />
        <activity
            android:name=".fragment.ActivityTaskPage"
            android:label="@string/title_activity_activity_task_page" >
        </activity>
    </application>

</manifest>
